# ============ 环境与密钥（绝不上传！） ============
.env
*.env.local
.env.*

# ============ Python ============
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# ============ 运行时数据（可重新生成，勿上传） ============
# 仅忽略根目录 data/（向量库/栅格/合成数据/OSM 缓存/中间产物/分析输出）
# 注意：backend/agent/data、backend/rag/data 是种子数据，需要入库，不受此规则影响
/data/

# ============ 工作记忆（个人笔记，勿公开） ============
.workbuddy/

# ============ 系统 / 编辑器 ============
.DS_Store
.idea/
.vscode/
*.swp

# ============ Docker（本机 CLI 配置/凭证/buildx 状态，勿上传） ============
.docker/
